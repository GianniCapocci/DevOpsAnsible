app:
  env:
    server.port: "{{app_port}}"
    spring.datasource.username: appuser
    spring.datasource.password: apppassword
    # spring.datasource.url: jdbc:postgresql://52.169.33.91:5432/appdb
    # Private IP address since it's in the same provider
    # spring.datasource.url: jdbc:postgresql://10.0.0.4:3306/appdb
    # spring.datasource.url: jdbc:mysql://devopsdb:3306/appdb
    spring.datasource.url: jdbc:mysql://10.0.0.4:3306/appdb